{
  "name": "ip",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --ip 0.0.0.0",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vitest": "1.3.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0"
  }
}
